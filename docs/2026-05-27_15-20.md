# 修改记录: 2026-05-27 15:20

## 诊断
Gen 5.1 训练 3000→7377 轮，核心成果 (feet_air_time 转正, episode_length >1900) 稳定保持，但 motion quality 出现周期性振荡：

```
4488 peak → 4995 crash1(smooth=-1.45) → 5762 recover → 6569 crash2 → 7377 crash3(smooth=-1.26)
```

smoothness 每 ~1500 轮大幅恶化后自行恢复，tracking 跟随波动。符合 CLAUDE.md 模式 5 "反复出现"：PPO 更新方差过大，单次 bad batch 导致剧烈参数变化，需要更多环境样本平滑梯度估计。

## 修改内容
| 参数 | 旧值 | 新值 | 原因 |
|------|------|------|------|
| `num_steps_per_env` | 48 | 72 | +50% 样本量/更新，更稳定梯度估计，抑制周期性崩溃 |
| `learning_rate` | 1e-3 | 5e-4 | 减小更新步幅，配合更大 batch 防止 overshoot |

## 预期效果
- smoothness 波动幅度减小，不再出现 -1.x 级崩溃
- tracking_lin_vel 稳定在 0.75-0.85 区间
- 收敛速度变慢但更稳定，episode_length 保持 >1900
